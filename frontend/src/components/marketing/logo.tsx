/**
 * Re-exported from the shared UI layer so marketing and application render the
 * identical mark. Kept as a module so existing marketing imports keep working.
 */
export { Logo, Wordmark } from "@/components/ui/logo";
